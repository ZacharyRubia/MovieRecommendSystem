{
  "name": "movie-recommend-system",
  "version": "1.0.0",
  "private": true,
  "description": "Movie Recommendation System - Monorepo with separate backend and frontend",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "start:backend": "cd backend && npm start",
    "dev:frontend": "cd frontend && npm run dev",
    "start:ai": "python scripts/recommend/recommend_api.py --port 5100",
    "dev": "concurrently \"npm run start:backend\" \"npm run dev:frontend\" \"npm run start:ai\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "ioredis": "^5.10.1"
  }
}
