# A/B 测试分析模块依赖
# 安装: pip install -r scripts\analysis\requirements.txt
#
# 覆盖设计文档 §5 — Python 离线分析
#   - MySQL 连接 (pymysql)
#   - Redis 连接 (redis)
#   - 统计计算 (scipy, numpy)

pymysql>=1.0.2
redis>=4.0.0
scipy>=1.7.0
numpy>=1.21.0
pandas>=1.3.0