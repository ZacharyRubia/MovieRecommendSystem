# MovieLens 推荐系统 - 训练模块依赖
# 基于稀疏矩阵 + 分块计算的优化版训练（SVD / User-CF / Item-CF）
#
# 安装: pip install -r requirements.txt

numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
scikit-learn>=1.0.0
threadpoolctl>=3.1.0  # 控制 BLAS 线程数, 避免嵌套并行导致线程爆炸
matplotlib>=3.4.0    # 可选, train_svd.py 绘图用
